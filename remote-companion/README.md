# Codex Notifier Remote Companion

Install this companion on the Remote SSH host while Codex Notifier remains
installed locally. It watches the remote `Codex.log` and forwards completed
turns to the local `codexNotifier.notifyComplete` command.
